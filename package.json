{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "boot": "lerna bootstrap",
    "build": "lerna run build",
    "test": "lerna run test",
    "test:util": "lerna run --scope @wynn721/util test",
    "clean": "lerna clean",
    "changed": "lerna changed",
    "publish": "lerna publish",
    "create": "node scripts/cli.js"
  },
  "devDependencies": {
    "@types/jest": "^27.0.1",
    "jest": "^27.1.1",
    "lerna": "^4.0.0",
    "rimraf": "^3.0.2",
    "rollup": "^2.56.3",
    "typescript": "^4.4.3"
  }
}
